
'use client';

import { AppLayout } from '@/components/layout/app-layout';

export default function HomePage() {
    return (
      <AppLayout>
        {/* Content is now determined by AppLayout based on role */}
      </AppLayout>
    );
}
